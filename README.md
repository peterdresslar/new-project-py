### new-project-py

A boilerplate generator for new python projects, simple and short. Does include jupyter since that seems to be a common need for me of late.

#### Usage
`pip install .`

or

`pip install editable .`